# HA connection (optional — offline mode works without these)
HA_URL=http://homeassistant.local:8123
HA_TOKEN=your_long_lived_access_token_here

# Server
HOST=0.0.0.0
PORT=8000
RELOAD=true

# Dashboard
CONFIG_PATH=config/simple.yaml

# Display resolution
DISPLAY_WIDTH=800
DISPLAY_HEIGHT=480
